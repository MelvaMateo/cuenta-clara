# Archivos del repo que no son parte del sitio web.
# PLAN.md es el documento del caso de estudio del capstone: queda en GitHub
# para la entrega, pero no hace falta publicarlo en la web.
PLAN.md

# Las pruebas y lo que instalan: el sitio es estático y se publica tal cual,
# sin instalar nada. Sin package.json a la vista, Vercel no corre npm install.
package.json
package-lock.json
node_modules/
tests/

# El pipeline de CI (GitHub Actions) tampoco es parte del sitio.
.github/

# Herramientas que se corren a mano, como el generador de imágenes.
scripts/

# La documentación de arquitectura vive en el repo, no en el sitio.
docs/

# Los reportes de cobertura van al repo (los pide el capstone), no al sitio.
coverage/
