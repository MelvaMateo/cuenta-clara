{
  "name": "cuenta-clara",
  "private": true,
  "description": "Solo para las pruebas: el sitio es estático y no instala nada para publicarse.",
  "scripts": {
    "test": "npm run test:sintaxis && npm run test:logica && npm run test:health && npm run test:sql && npm run test:ui",
    "test:sintaxis": "node tests/sintaxis.mjs",
    "test:logica": "node --test tests/logica.test.mjs",
    "test:health": "node tests/health.test.mjs",
    "test:sql": "node tests/sql.test.mjs",
    "test:ui": "node tests/ui.test.mjs",
    "cobertura": "node tests/cobertura.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "istanbul-lib-coverage": "^3.2.2",
    "istanbul-lib-report": "^3.0.1",
    "istanbul-reports": "^3.2.0",
    "puppeteer-core": "^25.10.0",
    "v8-to-istanbul": "^9.3.0"
  }
}
